# 2026-06-04 — 平台金库可见性修复 + 投票系统恢复

## Phase G: 平台金库始终可见
- **问题**: 金库栏条件 `treasury.gameCoins > 0 || treasury.aCoins > 0` 导致未产生佣金时不可见，用户无法确认功能存在
- **修复**:
  1. `useState<PlatformTreasury>(loadTreasury)` → `useState(() => loadTreasury())` 显式箭头包装，避免 React 将函数作为 lazy initializer 时可能的歧义
  2. 移除金库栏的 `> 0` 条件，始终在「浏览市场」标签页展示
  3. `totalTransactions > 0 ? 'N 笔交易' : '暂无交易'`

## 投票通知系统修复（续）
- **断裂点 1**: `gameProposalService.ts` 第23-25行桩函数覆盖 `generateSimulatedPlayers`/`generatePlayerMetrics`/`calculateVoteWeight` → 改为从 `@/data/simulatedPlayers` 正确 import
- **断裂点 2**: `VoteNotifications` (sonner toast 通知) 从未挂载 → 在 `App.tsx` `<AuthProvider>` 内添加 `<VoteNotifications />`
- **断裂点 3**: `VoteNotificationPanel` (完整投票面板：待投票/已投票/已结算+倒计时+权重) 从未挂载 → `PersonalCenter.tsx` 投票 Tab 替换 `VoteRecordPanel` 为 `<VoteNotificationPanel />`，删除旧的 `VoteRecordPanel` 函数
- 全部修复后 `pnpm build` 通过
- **根因**: MVP 重构时 `src/data/simulatedPlayers.ts` 被清空为桩函数（空数组/空对象/return 1），导致：
  1. `VoteVoucherService.startVoteCycle()` → `eligibleVoters.length = 0` → 抛出异常 → 投票凭证无法生成
  2. `VoteNotificationPanel` → 无通知显示
  3. `gameProposalService.finalizeProposal()` → `calculateResult()` 所有分组计数为 0 → 提案永远 REJECTED
- **修复**:
  - 从 `AllinONE Online/simulatedPlayers.ts` 恢复完整文件，将旧 5 个测试账号（user-001~005）替换为当前 4 个测试账号映射（test-001~004），保持 54 人模拟玩家池
  - test-001 (player1/冒险者小明) → PLAYER_COMMUNITY, voteWeight=1.5
  - test-002 (player2/探险家小红) → PLAYER_COMMUNITY, voteWeight=0.5
  - test-003 (dev1/开发者老张) → GAME_DEVELOPER, voteWeight=2.0
  - test-004 (admin/管理员) → PLATFORM, voteWeight=3.0, hasVetoRight
  - 保留完整动态权重引擎 (calculateVoteWeight / generatePlayerMetrics / getPlayersByType / getPlayerStats)
  - `npx tsc --noEmit` 零错误，`pnpm build` 通过

## 道具凭证使用标识
- **问题**: 道具凭证使用后无任何标记——消耗品直接 DESTROYED 消失，永久道具保持 ACTIVE 但转移所有权，用户无法区分已使用/未使用
- **修复**:
  1. `src/voucher-system/types.ts`: `VoucherStatus` 新增 `REDEEMED = 'redeemed'`，`TransactionType` 新增 `REDEEM = 'redeem'`
  2. `src/voucher-system/services/VoucherService.ts`: 新增 `redeemVoucher()` 方法（标记为 REDEEMED 而非销毁，保留追溯）
  3. `src/services/voucherItemService.ts`: `redeemItemVoucher()` 统一调用 `voucherService.redeemVoucher()`，删除 isConsumable 分支
  4. `src/pages/PersonalCenter.tsx`:
     - `VoucherAssetPanel`: 同时展示 ACTIVE + REDEEMED 凭证，已使用的显示半透明(opacity-60)+虚线边框+文字删除线+"已使用"琥珀色徽章
     - `ItemVoucherPanel`: 同上，新增"已使用的道具凭证"分隔区，底部统一展示
  - `pnpm build` 通过

## GameStore "我的凭证" 已使用标识
- **问题**: GameStore 的"我的凭证"tab 不区分已使用/未使用凭证，全部混在"待兑换"中
- **修复**: `src/pages/GameStore.tsx`
  - `myItemVouchers` 拆分为 `activeVouchers` 和 `redeemedVouchers`（IIFE 包裹）
  - 已使用凭证：半透明(opacity-60)+虚线边框+名称删除线+属性半透明+"已使用"徽章，兑换按钮替换为"该道具凭证已使用"提示
  - 标签页徽章只计 active；说明卡片显示"X 个可用凭证，Y 个已使用"
  - `pnpm build` 通过

## GameStore 兑换码区新增"标记已使用"
- **根因**: 购买后生成的凭证带有兑换码，UI 只显示"复制兑换码"——用户复制后去游戏中兑换，平台无入口标记已使用
- **修复**: `GameStore.tsx` 兑换码区域新增 amber 色调「已在游戏中兑换，标记为已使用」按钮，点击调用 `handleRedeemItemVoucher()` → `redeemVoucher()` 设 REDEEMED
- 同时统一旧版兑换按钮提示文案为："兑换后凭证将标记为已使用，保留在记录中可追溯"
- `pnpm build` 通过

## GameStore 钱包余额修复
- **问题**: GameStore 顶部 A币余额把道具凭证（sourceType=ITEM）也计入了，导致 A币数值虚高
- **根因**: `loadVoucherBalance()` 和 `handlePurchase` 凭证支付过滤只检查 `status==='active'`，未排除道具凭证
- **修复**: 导入 `isCurrencyVoucher`，两处均添加 `&& isCurrencyVoucher(sourceType)` 过滤
- **参考**: `voucherPaymentService.getUserVoucherBalance()` 已有正确过滤，GameStore 之前未保持一致
- `pnpm build` 通过

## Phase 1-3: 平台金库管理体系建设
### 用户需求
- 有平台佣金后需要平台账户 + 管理页面
- 确认：Phase 1/2/3 一起做；管理入口放首页建筑卡片；整合旧 /platform-store-manage；平台账户名 platform-treasury

### Phase 1: 平台账户
1. **`src/services/platformTreasuryService.ts`** (新建) — 平台金库管理服务
   - `depositCommission()`: gameCoins 走 `walletSkill.recharge()` 真实入账 system-platform 账户；aCoins 自然留存于凭证系统 platform_pool
   - `getReport()`: 返回余额、累计分类（P2P佣金/提案没收/凭证回收/平台手续费）、今日/本月收入
   - `getTransactions()`: 按来源分类的流水记录，支持 source 字段 (p2p_commission | proposal_forfeit | voucher_recycle | voucher_expire | platform_collect)
2. **`src/data/testAccounts.ts`** — 新增 `PLATFORM_ACCOUNTS` 常量（system-platform / platform_pool），所有测试账号添加 `role` 字段
3. **`src/services/marketplaceService.ts`** — 佣金存入从 `depositTreasury()` 改为 `await platformTreasuryService.depositCommission()`
4. **`src/voucher-system/engine/RuleEngine.ts`** — `'treasury'` 和 `'platform'` 分支统一转入 `'system-platform'` 账户（原来 treasury 转给不存在的 `'TREASURY'`，platform 分支为空实现）

### Phase 2: 平台管理中心页面
5. **`src/pages/PlatformAdmin.tsx`** (新建) — 管理员专属仪表盘，4 个 Tab:
   - 金库概览: 余额卡片 + 累计收入分类 + 今日/本月统计
   - 收入明细: 按来源分类（P2P交易佣金/提案押金没收/凭证规则回收/平台手续费）
   - 交易历史: 全部流水表格（时间/来源/货币/金额/描述）
   - 游戏商店管理: 嵌入 `PlatformGameStoreManager`（整合旧路由）
   - 权限守卫：当前用户 role 非 admin/platform 时显示无权访问
   - 自动 30 秒刷新
6. **`src/App.tsx`** — 新增 `/platform-admin` 路由；旧 `/platform-store-manage` 重定向到 `/platform-admin?tab=store`

### Phase 3: 管理员入口
7. **`src/pages/GameBase.tsx`** — 建筑卡片列表中，当 `currentUser.role === 'admin' || 'platform'` 时追加 `⚙️ 平台管理` 卡片

### 验证
- 全部 7 个文件 0 lint 错误
- `pnpm build` 通过
