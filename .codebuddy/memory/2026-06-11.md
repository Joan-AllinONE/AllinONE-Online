# 2026-06-11 工作日志

## Bug 修复：高级模式粘贴 JSON 被降级为中级校验

**问题**：AIDialog.tsx 的 `handlePasteSubmit` 和 `handleGenerate` 调用 `ugcBridgeService` 时未传 `tier` 参数，导致 `createFromJSON` 自动检测（无 effectScript → intermediate），用户选择的高级模式被忽略。

**修复**：两处调用传入当前 UI 的 `tier` 状态变量。

**文件**：`src/components/ugc/AIDialog.tsx`（2处 replace）
