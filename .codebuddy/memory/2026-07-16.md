# 2026-07-16 工作记录

## SOP 跨浏览器持久化修复（v1 → v2 → v3）— 已验证成功 ✓

**v1 失败**：用 `getTempFileURL({ fileList: [cloudPath] })` 加载 SOP → 匿名用户返回空/错误
**v2 失败**：改用 cloudFileID 格式 + getTempFileURL → 匿名用户返回 STORAGE_EXCEED_AUTHORITY
**v3 成功** ✓：用户确认"这次成功了"

**v3 根因修正**：
- `getTempFileURL` 对匿名用户返回 `STORAGE_EXCEED_AUTHORITY`（云存储安全规则限制），cloudFileID/cloudPath 都不行
- published game HTML 跨浏览器真正靠 `entryHtmlContent` 直接存 DB 文档（匿名可读 DB），不是 getTempFileURL
- SOP 复用此 proven pattern — sopDocument 直接存 DB 文档作为首要通道

**之前记录的错误纠正**：
- ❌ 旧记录称"CloudBase DB 写入永远失败"→ 正确说法是**写入失败但读取正常**（匿名可读 published_games 集合）
- ❌ 旧记录称"后端 API 是真正生效的方案"→ 正确说法是 **DB 文档字段（sopDocument）才是首要通道**，后端 API 只是辅助
- ❌ 旧记录称"getTempFileURL 正常运行"→ 正确说法是 **getTempFileURL 仅认证用户可用**，匿名返回 STORAGE_EXCEED_AUTHORITY

**v3 修复详情**（`src/services/publishedGameService.ts`）：
1. `loadSopFromCloudStorage` 新增 ⓪ 步：读 DB `sopDocument` 字段直接获取（proven pattern，匿名可读）
2. 加载优先级：⓪ DB sopDocument → ① 后端 API → ② 云存储 cloudFileID → ③ 云存储 cloudPath
3. `addSopToCloudManifest` 修复：游戏不在缓存时不回退，直接构建 manifest 写 DB + 同时写 sopDocument
4. `addSopToCloudManifest` 新增第 3 参数 `sopMd`，确保 sopDocument 内容也写入 DB
5. `savePublishedGame` SOP 保存：云存储失败时也写 sopDocument 到 DB + 后端 API
6. 注释更新为 v3 策略说明

**构建验证**：vite build 成功，lint 0 新增错误。
**用户验证**：跨浏览器 SOP 加载成功 ✓

---

## 部署：前端更新到 CloudBase 静态托管（2026-07-16 二次部署）

**执行**：node vite build → node 全局 tcb hosting deploy dist/static -e allinonegaming-d4gmsmrzz573264f6。10 文件全成功；新产物 `assets/index-BBnqALo9.js`（上次为 index-CRDEMYYb.js），验证首页已引用新 JS（CDN 已刷新）。

**说明**：仍为纯前端部署，`/api/v1/*` 后端在该静态托管域名下不可达（无 CloudRun 服务、无 SPA 转发）。后端全功能需把 `server.js` 部署为 CloudRun（`USE_MEMORY_DB=true` 简单/重启丢数据，或 PostgreSQL 持久化需先建库）。用户尚未确认后端部署方案。
