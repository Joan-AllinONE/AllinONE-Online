# 2026-07-20

## 活动中心模块集成收尾完成
- 清理 `ActivitySkill.ts` 未使用的 `SkillContext` 导入。
- 修复 `ActivityCenter.tsx` 向 `LotteryWheel` 误传的不存在 `progress` prop。
- 前端 tsc：活动模块相关文件 0 错误（codebase 其余既有 TS6133 未使用变量错误与本次活动无关，vite 用 esbuild 转译不类型检查故不影响构建）。
- 后端 tsc（`tsconfig.server.json --noEmit`）：活动模块 0 错误。
- `vite build --outDir dist/static` 成功，dist/static 已生成（assets/index.html/gameFileServiceWorker.js）。
- 模块此前已完成：EventBus 埋点（game.published/vote.cast/daily.login/user.registered/game.played）、ActivityTracker 动态 userId、ActivityCenter 路由(/activity)、GameBase 入口卡片、server activity 路由、activityService 本地 dev-token、defaultActivities 种子对齐 types.ts。
