# 2026-08-16

## 修复：独立 HTML 入口提交被 slot 校验拦截（用户报 bug）

用户用超级玛丽测试：原游戏只有第一关，做了第二关（一个含第一+第二关的完整 HTML 页），发布 level 任务、提交时上传 HTML，报错：
「slot "levels" 的 data 应为数组；data 缺少必需字段: id/title；文件 xxx.html 类型不允许（slot "levels" 允许: json）」

### 根因
P2 slot Schema 校验只面向「纯 JSON 数据扩展」（data 数组 + slot 白名单资产），
与 P1「独立 HTML 入口（entryPoint）」场景冲突，导致提交被前端预校验拦截。

### 修复（4 文件，双模式校验）
1. **`src/types/quest.ts`**：
   - 新增 `QUEST_WEB_ASSET_EXTS`（独立入口模式允许的 web 资源扩展名：html/css/js/json/图片/音频/视频/字体等）
   - `validateQuestContent` 改双模式：`entryPoint` 非空 → **独立入口模式**（跳过 data 结构/必填校验，资产放宽到 web 白名单）；否则走原严格校验
2. **`src/components/quest/QuestDetailModal.tsx`**：前端预校验调用补传 `entryPoint`（此前没传 → 永远走严格模式，是直接根因）
3. **`src/server/routes/quests.ts`**：
   - `validateContentPack`：独立入口模式资产跳过危险代码扫描（HTML 页必然含 `<script>`，扫描无意义；安全靠 iframe sandbox + 审核）
   - merge：data 为空对象/空数组时跳过追加（防污染 levels 列表）
4. **`cloudfunctions/gamesApi/index.js`**：同步以上全部（`QUEST_WEB_ASSET_EXTS` + `validateQuestSlot` 双模式 + 危险扫描跳过 + merge 空 data 跳过）

### 验证
后端 tsc ✅ / 云函数 node --check ✅ / 前端 lint 0 错误
（前端 tsc 全量仅剩既有 `standard-sdk/index.ts` 错误，非本次引入）

### 结论/约定
- **纯数据扩展**（无 entryPoint）：data 数组 + slot 白名单，游戏引擎自己解析
- **独立入口扩展**（有 entryPoint，上传 HTML）：data 可空，资产任意 web 资源，GamePlay 以 iframe 打开入口页
- 危险代码扫描仅对纯数据模式生效；独立 HTML 扩展的安全边界 = iframe sandbox + 人工审核

## 二次修复：提交记录仍 auto-failed 的真正根因 = server.js 跑旧编译产物（同日）

用户重测：HTML 提交「成功」（接口返回 success:true），但审核面板显示该记录 auto-failed，错误仍是旧逻辑的（eval/Function/script + data 数组 + html 类型不允许）。

### 根因（关键教训）
- `server.js` 从 **`./dist/server/server/routes/quests.js`（编译产物）** 加载路由，不是 `src/server/routes/quests.ts`！
- 首次修复只跑了 `tsc --noEmit`（仅类型检查、**不产出 dist 文件**）→ dev server 实际运行的仍是旧校验逻辑
- 提交接口即使 auto-failed 也返回 `success:true`（记录保留供人工审核），前端误显示「提交成功」

### 修复
1. **重新编译产物**：`node node_modules/typescript/bin/tsc -p tsconfig.server.json`（关键步骤）
2. **重启 dev server**：旧进程（node server.js）加载的 dist 是启动时定格的，必须 kill 后以同参数重启（USE_MEMORY_DB=true PORT=3000）
3. **前端 `QuestDetailModal.tsx`**：提交后检查 `r.data?.status === 'auto-failed'` → 显示校验失败原因，不再误报「提交成功」
4. 清理：删除 test-001 对该任务的旧 auto-failed 提交 + 注入的测试提交（`.data/memory-db.json` 中 `questSubmissions` 是数组），claim 降级为 active

### 端到端验证（真实 API）
- `POST /__quests/:id/submit` 带 entryPoint(HTML含`<script>`/`eval`) + data={} → **status: reviewing, autoCheck.passed: true** ✅
- 任务列表/详情正常；测试数据已清

### ⚠️ 约定（重要）
- **改 server 代码后必须重编译 + 重启**：`tsc -p tsconfig.server.json`（会产出 dist），且 dev server 进程要 kill 后重启，否则改动不生效
- 排查「线上与预期不符」先确认 dist 是否同步：grep `dist/server/server/routes/quests.js` 是否含新关键字
- 云函数（生产）代码同步修改后需 `tcb fn deploy gamesApi` 才生效

## 三次修复：合并后游戏不加载第二关 = GamePlay 只读旧缓存（同日）

用户：任务提交合并成功，但「更新后的游戏没加载第二关」，控制台显示加载的是「超级玛丽改造5.html」（原版），且报 `[AllinONE LocalPatcher] 游戏未暴露 allinoneAdapter`（此警告与扩展无关，是 Effect Engine 正常降级）。

### 排查确认（数据链路全部正常）
- merge 成功：`published_games[game-1786835238037].questExtensions` 有 1 条（entryPoint=extensions/quest_1786835304627/sub_xxx/超级玛丽改造6.html）
- 扩展文件已落库：game_files 中 game_id=game-1786835238037 有 extensions/.../超级玛丽改造6.html（87283 字节）
- `GET /api/v1/games/game-1786835238037/files/extensions/.../超级玛丽改造6.html` → 200 text/html ✅
- 后端详情 `GET /:id` 返回完整文档含 questExtensions ✅
- ⚠️ 游戏真实 id 是 `game-1786835238037`（published_games 是数组，node 输出的 0/1 是数组索引不是 id！）

### 根因
`GamePlay.tsx` 只调 `getPublishedGame(gameId)` **从本地缓存（localStorage/IndexedDB）同步读一次**，
而 merge 只写后端，前端缓存里 `questExtensions` 缺失/滞后 →
- URL 带 `?ext=<submissionId>`（审核面板「在游戏中查看扩展」链接）也匹配不到 → 回退原版入口（改造5.html）
- 头部「扩展 (N)」菜单不显示
`loadGameFromCloudBase`（后端详情）是私有函数，GamePlay 没调用。

### 修复（2 文件，前端）
1. **`src/services/publishedGameService.ts`**：新增导出 `refreshGameDetail(gameId)`——调后端详情接口拉最新数据（含 questExtensions），合并回缓存（保留本地未落云的 SOP 字段），返回最新对象
2. **`src/pages/GamePlay.tsx`**：
   - 提取 `computeEffectiveGame(publishedGame, extParam)` 纯函数（原 useEffect 内联逻辑：?ext 匹配 questExtensions / isModular → 强制 server 托管 + cdnUrl 指向扩展入口）
   - useEffect 末尾异步 `refreshGameDetail(gameId)` → 用最新数据重算 effectiveGame → setGame（仅当 questExtensions 变化时更新，避免无谓重渲染）

### 验证
前端 lint 0 错误 / tsc 仅剩既有 standard-sdk 错误
后端详情接口实测返回 questExtensions=1 ✅ / 扩展文件 URL 200 ✅

### 结论
- 任务扩展的消费路径：审核面板「在游戏中查看扩展」→ `/game/:id?ext=<submissionId>`，或游戏内头部「扩展 (N)」菜单
- 原版入口默认不变（改造5.html），扩展是独立入口（改造6.html），不会自动替换原版——这是设计（base 保留 + extension 追加）
- 用户需重新进入游戏页（触发 refreshGameDetail）即可看到扩展菜单/扩展版

## 方案 A：超级玛丽「数据驱动关卡加载器」实施完成（同日）

背景：用户问「玩家上传的不是完整 HTML/不完整游戏怎么办（如只有第二关代码）」→ 确认设计预期是 ContentPack 数据追加；落地游戏侧消费能力 = 方案 A 关卡加载器。

### 改动（3 文件，全部 0 lint）
1. **`src/types/quest.ts`**：`levels` slot 的 `allowedAssetExts` `['json']` → `['json','png','jpg','mp3']`，hint 补 layout 字符网格说明
2. **`cloudfunctions/gamesApi/index.js`**：JS 版 `QUEST_SLOT_SCHEMAS` 同步放宽
3. **`AllinONE Online/超级玛丽二创/超级玛丽改造5.html`**：新增 `A1G_EXT` 关卡加载器模块
   - `gameId` 来源：`PROTOCOL:INIT` postMessage（内联/srcDoc 唯一来源，平台 establishChannel 已下发）+ URL pathname 正则（server 模式）回退
   - `apiBase`：`tcloudbaseapp.com` hostname → 绝对云函数 URL，否则 `/api/v1/games`（CORS 已放开 `*`）
   - 启动时 `a1gLoadCustomLevels()` fetch 公开详情 → 过滤 `doc.levels` 中带 `layout` 的关卡
   - layout 格式：15 行字符网格（`#地面 B砖 ?金币 M蘑菇 U已用 X硬块 (管道左 )管道右 [管身左 ]管身右 C金币 F旗杆`）或二维数字数组；`cols/enemyCols/flag/flagCol/castleCol` 可选
   - `a1gApplyLevel`：重建 grid + 动态 COLS/LEVEL_W/FLAG_COL/CASTLE_COL/DOOR_X + enemySpawns；ROWS 固定 15（出生点/物理硬编码依赖）；底部两行地面兜底 + 末尾自动旗杆
   - `buildLevel()` 开头恢复标准尺寸（COLS=206/FLAG_COL=188 等）并重建 grid（否则扩展关卡后回原版会错位）
   - `resetLevel()`：`activeExt>=0` 时 a1gApplyLevel，否则 buildLevel
   - 标题界面：`#level-selector` 下拉（DOM）+ `↑/↓` 键盘切换，`panel` 面板加「🎮 关卡」行；Enter 开始按 selExt 决定关卡
   - 语法已用 `node --check` 验证

### ⚠️ 生效条件
- 改造后的 HTML 需**重新发布**为游戏文件（published_games/game_files）才生效；已发布旧版本不会自动更新
- 云函数注册表改动需 `tcb fn deploy gamesApi` 才在线上生效
- P0 仅键盘/下拉切换；触摸端用下拉（`#level-selector`）

## 方案 B（脚本关卡）+ 方案 C（HTML 片段壳）实施完成（同日）

用户确认 B/C 是 A 的补充（数据→脚本→页面片段），三态覆盖「只传第二关代码」。

### 方案 C：fragment 片段壳（5 处）
1. **`src/types/quest.ts`**：`ContentPack.fragment?: boolean` + 导出 `wrapHtmlFragment(content)`（零依赖纯函数，二进制前缀返回 null 不包裹；TS/JS 双维护）
2. **`cloudfunctions/gamesApi/index.js`**：`wrapHtmlFragment` JS 版 + merge ①b 处：`contentPack.fragment===true` 时把 entryPoint 资产 content 包壳覆盖写入同 path（⚠️ 内联计算 rawEP，勿引用 ①c 的 const，TDZ）
3. **`src/server/routes/quests.ts`**：import `wrapHtmlFragment` + merge ①b-1 同理包壳（files.find 改 content/size）
4. **`src/components/quest/QuestDetailModal.tsx`**：entryPoint 输入下方加 checkbox（fragmentMode state），提交 contentPack 带 `fragment`

### 方案 B：脚本关卡（仅超级玛丽 HTML）
- levels 过滤改 `(l.layout || l.script)`；`lv._prepared` 预构建结果
- 新增 `a1gEncodePath`（按段编码保留 `/`，支持中文文件名）、`a1gRunScript`（fetch + Blob URL + `<script>` 标签，**非裸 eval**）、`a1gPrepareScriptLevel`（调 `window.MarioLevel.build(api)`，api 暴露 setT/pipe/stairUp/stairDown/grid/T/ROWS/COLS/setFlag/setEnemies；底部地面+旗杆兜底存 `_prepared`）、`a1gSyncSel`（键盘/下拉统一切换，脚本关卡自动预加载）
- `a1gApplyLevel` 开头加 `_prepared` 分支（脚本关卡直接用预构建 grid/enemySpawns）
- `startGame`：脚本关卡未就绪（非 error）时先触发加载并忽略本次 Enter；失败降级原版
- title 面板显示脚本关卡状态（加载中…/加载失败/脚本关卡）
- 下拉 onchange + keydown ↑/↓ 统一走 `a1gSyncSel`

### 脚本接口约定（文档写入 HTML 注释）
```js
window.MarioLevel = { build: function(api){ api.setT(c,r,v); api.pipe(c,top); api.setFlag(fc); api.setEnemies([25,60]); } };
```

### ⚠️ 生效条件（同 A）
- 游戏 HTML 需重新发布；云函数需 `tcb fn deploy gamesApi`；本地 server 需 `tsc -p tsconfig.server.json` + 重启
- 三态总结：方案 A=layout 字符网格（纯数据）；方案 B=script 引 assets js（代码构建）；方案 C=fragment 片段壳（完整页面减负）

## 补充：levels slot 放行 js + 脚本关卡示例（同日）
- **`levels` slot `allowedAssetExts` 补 `js`**（方案 B 脚本关卡 assets 含 .js 才能过校验）：`['json','js','png','jpg','mp3']`，TS/云函数双同步
- 新增示例脚本关卡：`AllinONE Online/超级玛丽二创/level2.js`（暴露 `window.MarioLevel.build(api)`，演示 setT/pipe/stairUp/stairDown/setFlag/setEnemies；data 提交 `[{id:'lvl2',title:'第二关：空中花园',cols:150,script:'level2.js'}]` + assets 上传该文件，entryPoint 不填）
- node --check 通过；注意示例脚本避免含 `eval(`/`<script`/`new Function`（非入口模式 assets 会扫危险代码）

## 修复：提交 level2.js 报「允许: json」（同日）
- 现象：前端预校验放行（vite 热更新 quest.ts 已含 js），但服务端返回 auto-failed「文件 level2.js 类型不允许（slot levels 允许: json）」
- 根因：**补 js 后未重编译 server 产物**——`dist/server/types/quest.js` 里 levels 仍是 `['json','png','jpg','mp3']`；用户本地跑的是更早的旧 dist（显示 json）
- 修复：`node node_modules/typescript/bin/tsc -p tsconfig.server.json` → dist 已含 js ✅；**需重启 dev server**（旧进程加载的 dist 是启动时定格）；线上需 `tcb fn deploy gamesApi`
- ⚠️ 再次确认既有约定：改 `src/server` / `src/types/quest.ts`（server 也编译它）后必须重编译 + 重启 dev server；线上另需部署云函数

## 修复：重启后仍报「允许: json」= 3000 端口 server.js 旧进程没被杀（同日，第二弹）
- 现象：用户重启后仍报旧注册表「允许: json」（连 `['json','png','jpg','mp3']` 都不是，是最原始 `['json']`）
- 排查：本地 `dist` 校验函数实测正常（issues:[]，levels allowed 含 js）；`server.js` 明确 import `./dist/server/...`
- 真根因：dev 环境**前后端分离**——vite dev 在 **3001**（热更新→前端预校验最新放行），/api 代理到 **3000**；3000 被 `node server.js`（**PID 9228，7:35 启动**，早于编译）占用且从未重启 → 内存里是编译前旧 dist
- 修复：`Stop-Process -Id 9228` 释放 3000 ✅；需重启后端 `USE_MEMORY_DB=true node server.js`（注意 `dev:server` 脚本**不带** USE_MEMORY_DB；`pnpm run dev` 会先 build:server 但 dev:server 本身不设 env）
- ⚠️ dev 约定再更新：vite=3001、server=3000 分离；重启后确认 3000 监听进程的 StartTime 是新时间（`Get-NetTCPConnection -LocalPort 3000` / `Get-Process node`），否则仍是旧进程

## 修复：游戏文件 503「后端离线且本地无缓存」= SW IndexedDB 中文路径解码 bug（同日，第三弹）
- 现象：新发布游戏（后端 500 时只写本地）打开 → iframe 请求 `/api/v1/games/{id}/files/超级玛丽改造5.html` → SW 后端 404/500 → Cache 无 → IndexedDB 兜底失败 → 503
- 根因①：**后端没起**（vite 3001 活着，3000 的 server.js 被 kill 后 concurrently 不自动重启）→ 所有 /api 500。已用 `Start-Process node server.js`（USE_MEMORY_DB=true，日志重定向 .data/server.*.log）拉起，PID 2708 端口 3000 ✅
- 根因②（真正 bug）：`URL.pathname` 保留百分号编码，SW `parseGameFileUrl` 得到 filePath=`%E8%B6%85...`（编码），而 publishedGameService.saveGameFiles 写 IndexedDB 的是**解码路径**（`超级玛丽改造5.html`）→ `serveFromIndexedDB` 精确/后缀匹配全部失败 → 503。老游戏能玩是因为文件在后端（后端优先命中），从未触发 IndexedDB 兜底
- 修复：`serveFromIndexedDB` 对 filePath 做 `decodeURIComponent`（try/catch），匹配逻辑同时覆盖编码/解码、忽略前导斜杠、后缀匹配；MIME 用解码路径。**SW bump v10→v11**（复制 v11 改 main.tsx 注册路径，删 v10，node --check 通过）
- ⚠️ 生效：浏览器需刷新 1-2 次让 v11 SW 安装激活（skipWaiting+claim）；**推荐重新发布一次游戏**（后端现在在线可写 game_files + 详情），SW 后端优先命中且跨浏览器可用
- 关联 memory「次生 bug：saveGameFiles 小游戏写 localStorage」——本次已改为始终写 IndexedDB，但 SW 读不到的根本原因是编码不匹配（非小游戏）

## 修复：js 拓展「0 个扩展关卡」= levels 嵌套数组 + 脚本路径解析（同日，第四弹）
- 现象：合并成功（后端 levels 有 lvl2），但游戏 `[Mario] 扩展关卡加载器: 0 个扩展关卡`
- 根因①：**merge 把数组 data 整体 push** → `slotArr.push(rawData)`，rawData 是 `[{lvl2}]` 数组 → levels 变 `[[{...}]]` 嵌套。游戏加载器 `levels.filter(l => l.layout||l.script)` 每个元素是数组 → 0 个。verify：vite 代理详情返回 `"levels":[[{...}]]`
- 根因②：**脚本关卡路径**。data 里 `script:'level2.js'`（相对路径），但 merge 后 assets 落库到 `extensions/{questId}/{submissionId}/level2.js` → 直接 fetch 相对路径 404
- 修复：
  1. 游戏加载器 `a1gLoadCustomLevels`：levels **扁平化一层**（兼容历史 `[[...]]`）+ 收集 `A1G_EXT.assetPaths`（questExtensions.assets 全量路径）+ 新增 `a1gResolveScriptSrc(lv)`（相对路径→按文件名匹配 assetPaths 完整路径）；`a1gPrepareScriptLevel` 改 `a1gRunScript(a1gResolveScriptSrc(lv))`
  2. **merge 展开追加**（云函数 + 本地 quests.ts）：`Array.isArray(rawData)` 时逐元素追加 + 逐元素去重（防未来嵌套）；object data 保持原逻辑
  3. 实测验证：`POST /api/v1/games/__files`（公开无 JWT）把最新改造版 HTML 写回后端 game-1786839957741 → 文件 200（含 a1gResolveScriptSrc）；level2.js 完整扩展路径 200（2201 字节含 MarioLevel）
- ⚠️ 生效：后端 `tsc -p tsconfig.server.json` 已重编译（merge 修复需重启后端 2708 生效）；游戏 HTML 已用 API 直接更新到后端（无需 UI 重新发布）；云函数需 `tcb fn deploy gamesApi`
- ⚠️ 已知：旧嵌套 levels 数据留在库里（游戏侧扁平化兼容）；新 merge 不再嵌套；后续新提交走新逻辑

## 修复：iframe 扩展菜单选「数据型扩展」无法进入 = ?ext= 需透传游戏侧自动选关（同日，第五弹）
- 现象：游戏内选第二关能玩（加载器 1 个关卡），但从 GamePlay header「扩展 (N)」选数据型扩展（entryPoint 空）→ URL `?ext=sub_xxx` → 只加载原版、不自动进第二关
- 根因：`computeEffectiveGame` 匹配 `e.submissionId===ext && e.entryPoint`，数据型扩展 entryPoint 为空 → 匹配失败 → 回退原版入口，无任何关卡定位
- 修复（4 处）：
  1. **`GamePlay.tsx`** `computeEffectiveGame`：匹配改为只看 `submissionId`；entryPoint 空（数据型）→ 加载原版入口 + 把 `?ext=` **透传 iframe URL**（`files/入口?ext=sub_xxx`）；render 侧高亮同步放宽
  2. **游戏加载器** `a1gAutoSelectFromUrl()`：读 `location.search.ext` → 按 `lv._submissionId` 精确匹配，回退按脚本文件名匹配该 submission 资产路径（`assetPaths`）→ `a1gSyncSel(idx+1)` 自动选中；`a1gLoadCustomLevels` 完成后调用
  3. **merge 写 `_submissionId`**（云函数 + 本地 quests.ts）：数组展开追加时 `{...item, _submissionId: subKey}`（旧数据无此字段，靠游戏侧文件名回退）
  4. 后端文件已用 `POST /__files` 更新 `game-1786843251805` → 94711 字节含 a1gAutoSelectFromUrl ✅
- 生效：前端 vite 热更新；后端文件已更新无需重启；merge 改动需重启后端 + 云函数 `tcb fn deploy gamesApi` 才生效（当前数据不依赖 _submissionId）

## 任务广场新增「任务发布指南」（同日）
- 新增 `src/components/quest/QuestPublishGuide.tsx`：可折叠面板（lucide BookOpen 图标 + 渐变横幅），含三态扩展模式（A 数据关卡 layout 字符网格 / B 脚本关卡 window.MarioLevel.build(api) / C 完整页面或 HTML 片段）+ 6 条踩坑总结（「允许: json」旧编译产物、SW 缓存强刷、0 个扩展关卡、503 文件不可用、扩展菜单不进关、script 字段文件名）+ 发布前检查清单
- `QuestPlaza.tsx`：mainTab==='quests' 分支顶部引入 `<QuestPublishGuide />`
- 0 lint；vite dev 热更新即生效（无需重启后端）
- ⚠️ 指南为前端展示组件，无需部署云函数/后端

## 扩展资产「路径回退」+ 指南通用化 + RPG 二创补文件场景（同日）
- 场景：用户测试 RPG 二创——index.html 引用 `js/gun.js` 但文件被删，想通过任务发布补回
- 实现「扩展资产回退」：请求原路径（如 `js/gun.js`）根目录没有时，回退匹配 `extensions/{questId}/{submissionId}/js/gun.js`（取最新合并），游戏零改动即可恢复被删文件
  - `src/server/routes/games.ts` GET files：memory 分支用 getGameFileManifest 过滤；PG 分支 SQL LIKE 'extensions/%'（已重编译）
  - `cloudfunctions/gamesApi/index.js` GET files：精确匹配 → 扩展资产后缀匹配（filter+pop 取最新）→ 最后才 index.html 兜底（修正了此前找不到文件直接回 index.html 的隐患，会把 js/gun.js 错返成 HTML）
  - 验证：`GET /files/level2.js`（原路径）→ 200 命中扩展资产；不存在的文件 404 ✅
  - ⚠️ 后端已重启（PID 11068）生效；云函数需 `tcb fn deploy gamesApi`
- 指南重写为**通用版**（QuestPublishGuide.tsx）：四种形态 = A 数据型 / B 代码脚本型（示例：超级玛丽 MarioLevel、RPG UGC effectCode）/ C 页面与片段 / D 修复补文件（RPG js/gun.js 示例，assets path 填游戏内相对路径，平台回退自动命中）；保留平台级踩坑 6 条 + 通用提交 schema
- RPG 二创结构备忘：index.html 引用 data/gamedata.js + js/{core,world,state,entities,main,gun,pet,mine}.js（gun/pet/mine 需存在），AllinONE 集成 = EFFECT_HANDLERS + window.__ALLINONE_EFFECT_DEFS + effectCode 沙箱 + useCustomPowerUp；allinone-sop.json 已含散弹枪/召唤橘猫/地雷等 UGC 道具

## 修复：补 gun.js 无论 fix/scripts 方案都加载不了 = 上传丢失目录前缀（同日，第六弹）

用户报：两种方案（fix 只传文件 / scripts 带 name+传文件）都 merge 成功，但游戏请求 `js/gun.js` 始终 404 → SW 兜底 503「游戏文件不可用」。

### 根因（信息丢失链路）
- 前端 `QuestDetailModal.importFiles`：用户**用文件选择器选单个 `gun.js`**，`webkitRelativePath` 为空 → 资产 path 退化为主文件名 `gun.js`，丢了 `js/` 目录前缀（只有拖入文件夹/上传 ZIP 才保留目录结构）
- merge 落库到 `extensions/{questId}/{submissionId}/gun.js`（不是 `.../js/gun.js`）
- 后端扩展回退此前只做「尾部完整路径匹配」`p.endsWith('/js/gun.js')` → 匹配不上 → 404
- 实测：内存库 manifest 里 2 个扩展文件路径都是 `extensions/.../gun.js`（16010 字节），但请求 `js/gun.js` 404，数据其实一直在库里

### 修复（3 文件 + 重编译重启）
1. **`src/server/routes/games.ts`**：新增 `matchExtensionPath(p, target)` 三级匹配 = 完全等于 → 尾部完整路径 → **文件名兜底**（basename 相等，`.../gun.js` 响应 `js/gun.js`，最新合并胜出）；memory/PG 两分支统一使用
2. **`cloudfunctions/gamesApi/index.js`**：GET files 扩展回退同样加 basename 兜底
3. **`src/components/quest/QuestDetailModal.tsx`**：资产草稿列表路径改**可编辑 input**（`gun.js` 可改 `js/gun.js`）+ 上传区加琥珀色提示（子目录文件需补全路径），根治信息丢失

### 验证（真实 API）
- `tsc -p tsconfig.server.json` EXIT=0 ✅；0 lint
- 重启整个 dev 树（kill pnpm run dev 全家：12140/5400/7704/5692/11068 → `Start-Process node .../pnpm.cjs run dev` 日志重定向 dev-server.log/.err.log）
- `GET :3000 与 :3001 /api/v1/games/game-1786869471045/files/js/gun.js` → **200**（14727 字符，含中文注释的 gun.js）✅
- ⚠️ 注意：`.env` 已有 `USE_MEMORY_DB=true`，`node server.js` 直接生效；重启后 `Get-NetTCPConnection -LocalPort 3000` 确认新 PID（14200）

### 约定
- **扩展回退匹配 = 三级**（精确→尾部→basename），本地 games.ts 与云函数需保持一致；云函数需 `tcb fn deploy gamesApi` 才在线上生效
- 玩家上传单个文件天然丢目录结构 → 要么改路径（UI 已支持），要么依赖 basename 兜底；同名文件多时最新合并胜出

## 实施：纯文件补丁校验宽松化（同日，用户追问我之前提的建议是否改了）

用户问「之前说的校验逻辑缺陷是否已改」→ 检查确认**未实施**（此前一直在修 gun.js 加载），本次落地。

### 改动（2 文件，前后端同步）
- `src/types/quest.ts` validateQuestContent + `cloudfunctions/gamesApi/index.js` validateQuestSlot：
  - requiredKeys 检查条件加 `&& !(dataIsEmptyObj && hasAssets)`（data 为空对象且提供 assets 时跳过必填字段校验）
  - `dataShape` 检查保留（防 levels 等需数组 slot 用空 data 蒙混）
  - 纯文件补丁（fix 补回被删文件）从此任何 slot 下「空 data + 有 assets」都能提交，不必被迫在 data 里填 name

### 验证（真实 API + 单测）
- dist 单测：`scripts+data{}`+assets → `[]` 通过；`scripts+data{}` 无 assets → 仍报「data 缺少必需字段: name」；`levels+data{}` → 仍报「data 应为数组」（dataShape 保留）✅
- 真实 HTTP：POST `__quests/quest_1786869610498/submit`（scripts，open）→ 空 data+有 assets = **reviewing/passed:true**；空 data+无 assets = **auto-failed** ✅
- 清理测试提交（无 DELETE 接口 → 停服后直接编辑 `.data/memory-db.json` 删 `questSubmissions` 中 test-003 的 2 条，再重启 dev）
- gun.js 修复无回归（仍 200, 14727 字符）
- dev 树已重启（后端 3000 PID 8112 / vite 3001 PID 10164）；云函数需 `tcb fn deploy gamesApi` 才在线上生效

## 指南同步更新（同日）
`src/components/quest/QuestPublishGuide.tsx` 按本轮两项行为变化更新（纯前端组件，vite HMR 生效，无需编译/重启）：
- 核心原则新增「零代码优先」说明（A 数据 / D 补文件 / 引用预设效果三类不用写代码）
- 模式 B 标题改「进阶·自定义新行为（可选，需写脚本）」+「不会写代码的玩家请忽略本模式」
- 模式 D 重写：开头黑体「补文件 = 只需上传文件 + data 留空对象即可，全程不写代码」；说明任何 slot 支持纯文件补丁（不再强制填 name）；标注三级回退（完整→尾部→文件名兜底，只传 gun.js 也能命中）+ 上传列表路径可点击修改
- 踩坑「补文件后仍加载失败」更新（先确认 merged，平台有文件名兜底，强刷 SW）；检查清单加「补文件类任务 data 留空即可」一条
