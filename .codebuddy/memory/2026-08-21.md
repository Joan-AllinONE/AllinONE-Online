# 2026-08-21

## 主页导航增加「任务广场」入口

- 需求：主页加入任务广场，位置在「活动中心」旁边
- 改动：`src/pages/GameBase.tsx` 的 `buildings` 数组，在 `活动中心`（route `/activity`）后新增 `{ icon: '📋', label: '任务广场', description: '接单创作赢报酬', route: '/quests' }`
- 路由 `/quests` 已存在（App.tsx → `<Route path="/quests" element={<QuestPlaza />} />`），页面 `src/pages/QuestPlaza.tsx`
- lint 0 错误

## 任务报酬改造：从游戏内钱包改为平台钱包（大重构）

### 需求（用户确认）
删除 gameWalletService（game_wallets 游戏内多币种钱包 computingPower/diamonds/gameCoins），任务报酬改为**平台钱包**：游戏币 `users.gameCoins` + A币凭证（vouchers）。奖励字段二选一（gameCoins/aCoins），删掉算力、钻石。沿用**后端合并发放**，打通 users 写权限。

### 双后端改造（本地 quests.ts + 云函数 index.js 同步）
- **`src/types/quest.ts`**：`QuestReward` → `{ gameCoins?, aCoins?, voucherTemplateId? }`
- **本地 `src/server/routes/quests.ts`** merge：写平台钱包（`memoryDB.addGameCoins` 加 users.gameCoins + `memoryDB.createACoinVoucher` 发 A币），替换原 game_wallets 多币种
- **云函数 `cloudfunctions/gamesApi/index.js`** merge：同样改——`db.collection('users').where({_openid}).get()` 读用户文档→加 gameCoins→`doc(id).set()` 写回（绕过通用隧道 READ_ONLY，仅在该 handler 内打通）；`gsAppend('vouchers', currency 凭证)` 发 A币。⚠️ READ_ONLY_COLLECTIONS 只拦通用 `/api/v1/games/:collection` 隧道，handler 内部可直写 users
- **`src/server/memoryDatabase.ts`**：新增 `users`/`vouchers` 内存存储 + 持久化（serialize/loadFromDisk/clear）+ 方法 `getUser`/`addGameCoins`/`createACoinVoucher`/`getUserVouchers`（dev 本地对标云函数）

### 前端
- **删除 `src/services/gameWalletService.ts`**（唯一被 TaskRewardsDrawer 用，已重写）
- **`TaskRewardsDrawer.tsx`** 重写：读平台钱包——`loadFromBackend('users')` 取 gameCoins + `_walletHistory`，`loadFromBackend('vouchers')` 汇总 A币（currentHolderId=userId & status=active），去掉了按游戏 game_wallets 明细
- **`QuestCreateModal.tsx` / `QuestPlaza.tsx`**：奖励输入框改为 游戏币+A币 二选一（删算力/钻石）
- **`QuestCard.tsx` / `QuestDetailModal.tsx`**：奖励展示改为 游戏币+A币（删算力/钻石图标 Cpu/Gem）

### 踩坑/注意
- A币凭证结构：`vouchers` 文档 `{ id, serialNumber, denomination, currentHolderId, status:'active', sourceType:'instant', category:'currency', metadata:{name,reason}, transferCount }`
- 平台游戏币字段：`users.gameCoins`（默认文档 gameCoins:0 + instantVouchers/algorithmVouchers）
- ⚠️ 前端 client 构建被**已存在的** `src/publishing-center/standard-sdk/index.ts:793` `export d` 语法错误阻塞（与本次改动无关，属历史损坏文件）

### 修复：standard-sdk/index.ts:793 `export d` 语法错误（阻塞构建）
- 根因：`index.ts` 末尾 793 行是损坏的孤立 `export d`（无效导出语句），导致 vite/esbuild 无法打包
- 修复：`export d` → `export default AllinONEGame;`（主类第 110 行 `export class AllinONEGame`，具名导出已完备，补默认导出兼容 `import AllinONEGame from '@allinone/standard-sdk'`）
- 验证：vite build 从阻塞 → exit 0（`vite v6.3.5 building for production`）
- 注：tsc 剩余 TS6133 未用变量 / TS7053 / TS2322 均为历史既有警告，不阻塞 vite build（esbuild 忽略未用变量），与本次无关
- `gameWallet.ts`（__wallet 隧道）保留——游戏内 SDK 自己的多币种钱包仍用，仅任务报酬不再依赖
