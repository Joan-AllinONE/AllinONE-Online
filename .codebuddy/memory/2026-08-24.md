# 2026-08-24 自检记录：云函数 + 跨浏览器数据同步

## 自检结论
### 已修复
- `src/services/backendSync.ts` 的 `SyncCollection` 类型补齐 `platform_config`、`store_products`，与云函数 `SYNC_COLLECTIONS` 白名单对齐（消除类型漂移；platformConfigService 调 `loadFromBackend('platform_config')`/`saveToBackend('platform_config',...)` 此前靠 `as SyncCollection` 绕过类型检查）。

### 🔴 阻塞级（环境）
- **云函数已欠费停机**：`GET /api/v1/games/dev-token` 返回 `FUNCTIONS_INVOCATION_FAILED / AvailableStatus = InsufficientBalance`。
- 影响：所有跨浏览器后端通道（含任务报酬 `_quests` 隧道、platform_config、published_games）当前全部不可用。需充值/恢复云函数环境才能联调验证。

### 🟠 代码级待修（需云函数配合，非纯前端）
- `WalletSkill.adjustBalance` 余额/交易写入依赖已损坏的 CloudBase JS SDK（`auth.call is not a function`）：
  - getBalance/adjustBalance 顶部 `db.collection('users')...get()` 抛错 → 进 catch → 仅写 localStorage，**线上不落库、不跨浏览器**。
  - writeQueue 降级分支（users 更新/创建 + transactions upsert，行230/383/410/450）仅在正常 SDK 路径触发，SDK 坏时根本走不到 → 线上失效。
  - InventorySkill 写 `inventories` 走 writeQueue 属"只读集合维持现状"设计，非 bug。
- 修复方向：WalletSkill 余额变动应改走云函数 admin 隧道（复用 `_quests` handler 直写 `users` 模式，或新增余额写端点），而非失效的 JS SDK/writeQueue。当前 backendSync 对 `users`/`transactions`/`inventories` 写操作 READ_ONLY 拦截（正确，防篡改），故不能简单改 backendSync。

### ✅ 确认正确（符合 memory 铁律）
- server.js 隧道路由在 gamesPublicRouter 之前；SW 注册 v14 与 main.tsx 一致；config.js / publishedGameService API_BASE（prod 绝对URL / dev 相对）正确；dev-token 端点；云函数 `let path` 可重写；resolveUserId 优先 Authorization token 回退 body；READ_ONLY 拦截 READ_ONLY_COLLECTIONS。
- 云函数 `node --check` 语法 OK。

## 遗留风险
- WalletSkill 余额跨浏览器同步为已知失效点，待云函数恢复后立项修复。

---

## 2026-08-24（续2）dev 数据泄漏到线上 排查 + 修复
### 根因（按严重性）
1. **🔴 backendSync.ts `API_BASE` 硬编码绝对生产云函数 URL**（第19行）：所有 `saveToBackend/loadFromBackend/deleteFromBackend` 的跨浏览器集合（published_games/vouchers/users/platform_config...）**无论 dev 还是 prod 全部打线上**。这是 dev 游戏元数据进线上的核心泄漏源。
2. `publishedGameService.saveGameFiles` 第①步云存储上传门控 `isCloudSyncEnabled()`：`VITE_CLOUD_SYNC_ENABLED=true pnpm run dev`（localhost）联调时解锁云存储上传 → 文件传**线上云存储**，污染共享存储。
3. 用户在线上静态托管域名(tcloudbaseapp.com)误当 dev 操作 → 运行在 prod 域名即写线上（代码无法区分，需 env 标记）。
4. SW/config.js：dev localhost 下 `base=''` 隔离正确，**不是**泄漏源（已确认）。

### 已修复
- `backendSync.ts`：`API_BASE` 改 hostname 判断 = `isCloudHosting() ? 绝对云函数URL : '/api'`（dev 相对走本地 server.js，与线上完全隔离）。`DEV_TOKEN_PATH` 保持相对 `/v1/games/dev-token`。
- `publishedGameService.ts`：新增 `isProductionTarget() = isCloudSyncEnabled() && /tcloudbaseapp\.com$/.test(location.hostname)`；`saveGameFiles` 第①步云存储上传门控由 `isCloudSyncEnabled()` 改为 `isProductionTarget()`（防 localhost+override 联调写线上云存储）。
- 其余通道（saveGameFiles ③__files、PublishingPipeline upload、savePublishedGameToBackend/patchGameOnBackend）均走 `GAMES_API_BASE`/`getApiBase()` = hostname 判断，dev localhost 相对→本地 server，已隔离，未改。

### 验证
- 三文件 lint 0 error（publishedGameService 892 行 `err` 未读为预存在 warning，非本次引入）；云函数 `node --check` OK。

### 仍待办（用户决策）
- 引入显式 `import.meta.env.VITE_APP_ENV` 环境标记 + 发布前二次确认，彻底区分 dev 预览部署与 prod 正式（当前仅靠 hostname 判断，线上域名=prod 无法区分"dev 预览"）。
- `saveSopToCloudStorage`（内容工坊）云存储上传仍依赖 `isCloudBaseReady`，`VITE_CLOUD_SYNC_ENABLED=true` 联调会写线上云存储（与 saveGameFiles 同类，尚未门控）。
- 整体前端 build 仍被 `src/publishing-center/standard-sdk/index.ts:793` 阻塞（与本次无关）。

---

## 2026-08-24（续3）环境隔离加固 + GameQuest 同步确认

### 执行（用户同意的建议）
1. **统一环境标识** `src/services/cloudbase.ts`：
   - 新增 `getAppEnv()`：`VITE_APP_ENV`(development/development 或 production/prod 显式优先) → 否则按 hostname（tcloudbaseapp.com=production）推断。
   - 新增 `isProductionTarget()` = `getAppEnv()==='production' && isCloudSyncEnabled()`（远程写唯一真相，替代分散的 isCloudHosting/isCloudSyncEnabled 组合）。
2. **publishedGameService.ts**：移除本地 `isProductionTarget` 重复定义，改 import 统一版；`saveSopToCloudStorage`（内容工坊）云存储上传门控由 `isCloudBaseReady()` 改为 `isProductionTarget()`（防 dev/联调写线上云存储）。
3. **PublishingCenter.tsx** `handlePublish`：prod 环境发布前 `window.confirm` 二次确认（"将发布到线上"）；发布按钮旁加环境徽标（🌐线上/🧪开发），用 `getAppEnv()`。
4. **VITE_APP_ENV 注入约定**：勿写进 .env（全局污染）；prod 构建注入 `VITE_APP_ENV=production`，dev 预览部署注入 `VITE_APP_ENV=development`。

### GameQuest 跨浏览器同步确认（已实现，无需改代码）
- 前端 `src/services/questService.ts`：`getApiBase()}/__quests` 隧道（dev localhost 相对→本地 server，prod 绝对→云函数）。
- dev：`server.js` 挂载 `/api/v1/games/__quests` → `createQuestsRouter(USE_MEMORY_DB, memoryDB,...)` 内存库，与线上隔离，多浏览器走同一 server 同步 ✓。
- prod：云函数 `GAME_SERVICE_HANDLERS.quests`（admin SDK 直写 quests/quest_claims/quest_submissions）→ 跨浏览器 ✓。
- 任务报酬走 `addGameCoins`/`createACoinVoucher` 直写 `users`（跨浏览器生效，memory 2026-08-21 已记录）。
- ⚠️ `questService.ts` 第14行 `API_BASE` 模块加载时计算 `getApiBase()`，运行时 hostname 不变则无碍。

---

## 2026-08-24（续）WalletSkill 改走后端隧道（已实施）
云函数停机无法充值，故先改代码。

### 改动
1. **云函数** `cloudfunctions/gamesApi/index.js`：`wallet` handler 内新增 `seg[0]==='platform'` 分支（在 `_quests` 同类 handler 内直写，绕过 READ_ONLY 隧道路径）：
   - `GET /__wallet/platform/balance` 跨浏览器读 `users.gameCoins` 等
   - `GET /__wallet/platform/transactions` 读 `transactions`
   - `POST /__wallet/platform/adjust` 写 `users.gameCoins`+`transactions`，**userId 仅取 token 解析值**（防越权），txId 幂等，余额不足 400
2. **前端** `src/skills/wallet/WalletSkill.ts`：`adjustBalance` 主路径改为调后端 `__wallet/platform/adjust`（Bearer token，getApiBase+getToken），成功则写回 localStorage+本地交易；失败回退 localStorage/SDK 兼容路径。import 增 `getApiBase`/`getToken`。
3. **dev** `src/server/routes/gameWallet.ts`：新增 `/platform/balance`、`/platform/transactions`、`/platform/adjust`（对齐云函数，用 memoryDB）。
4. **memoryDB** `src/server/memoryDatabase.ts`：新增 `adjustPlatformBalance(userId,delta,reason,txId)`（正负+幂等+`_walletHistory`）。
5. `src/services/backendSync.ts`：`SyncCollection` 类型补 `platform_config`/`store_products`（与云函数白名单对齐）。

### 注意
- `getBalance`/`getTransactions` 仍走 `readBalanceFromBackend`（loadFromBackend 读 users/transactions，读不受 READ_ONLY 限制）→ 跨浏览器读已通（前提是后端可达）。
- 整体前端 build 仍被历史损坏文件 `src/publishing-center/standard-sdk/index.ts:793` 阻塞（与本次无关）。
- **待云函数充值后验证**：prod `__wallet/platform/adjust` 实际落库 + 跨浏览器同步。
- dev 联调：`USE_MEMORY_DB=true node server.js` 下 WalletSkill 调 `/api/v1/games/__wallet/platform/adjust` 走内存库。
