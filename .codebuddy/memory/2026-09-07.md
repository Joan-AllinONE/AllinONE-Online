# 2026-09-07

## 修复：任务广场奖励发放后钱包余额不变
- 根因 1（游戏币）：云函数 gamesApi merge 发游戏币用 `doc(String(submitterId)).set(userDoc)`，注册用户文档 `_id` 是自动生成的 ≠ submitterId → 加币写到 `_id=submitterId` 新副本，原钱包文档不变，且产生重复 `_openid` 文档。
- 根因 2（A币）：merge 发的 A币凭证只写云端 `vouchers` 集合，钱包页 `useWallet` 的 voucherBalance 只读本地凭证库 → 云端凭证不可见。
- 修复：
  - `cloudfunctions/gamesApi/index.js` merge 报酬：按查到的真实 `_id` update（附 `_walletHistory` 追加），无文档走 `add`。
  - `src/hooks/useWallet.ts`：A币余额并入 `loadFromBackend('vouchers')`（currentHolderId=uid + active + 本地凭证 id 去重）。
  - `src/skills/wallet/WalletSkill.ts`：getBalance/adjustBalance/readBalanceFromBackend 改 `limit(10)` 取 `updatedAt` 最新文档，兼容历史重复文档。
- 验证：node --check 云函数语法 OK；vite build 成功（1m05s）；lint 0 错误。
- 部署：`tcb fn deploy gamesApi --force` ✔ + `tcb hosting deploy ./dist/static /` ✔（CDN 几分钟内刷新）。
- 备注：审核 approve ≠ 发放，须再点「合并并发放报酬」触发 merge 才发奖（设计流程，UI 有该按钮）。

## 修复：改造6 不支持任务扩展（A1G 加载器移植）+ SW dev-token 400
- 诊断结论（前一 session）：①超级玛丽改造6.html 完全没有 A1G 扩展加载器（只有改造5 有）→ levels 数据型合并后无人消费；②用户的 level3.js 是注入模式写法却按数据型提交（无 `inject:true`）→ 脚本永不执行；③`/dev-token` 400 = SW 内 `fetch(req)` 转发 POST 丢 body → token 拿不到 → resolveUserId 回退 body.submitterName → 提交者记成 `anonymous` → dev 下奖励发错人。
- 改造：
  - `AllinONE Online/超级玛丽二创/超级玛丽改造6.html`：从改造5 移植完整 A1G 加载器（A1G_EXT + a1g* 全套约 280 行，插在 spawnEnemies 后）+ 6 处集成点：body 头 level-selector HTML、resetLevel activeExt 分支、startGame 脚本就绪等待+activeExt、draw title 分支（选关器可见性+关卡名+加载状态）、keydown 标题 ↑/↓ 选关、主循环后 `a1gLoadCustomLevels()`。3 个内联 script `new Function` 语法校验通过。
  - 新增 `AllinONE Online/超级玛丽二创/level3.js`：数据型契约版第三关脚本（`window.MarioLevel={build(api)}`，api.setT/pipe/stairUp/stairDown/setFlag/setEnemies + api.grid 直写；原用户的注入式脚本不适合数据型提交）。食人花列对齐管口 24/60/98。
  - SW v17（`public/gameFileServiceWorker-v17.js`）：新增 `sameOriginFetch()`（非 GET/HEAD 用 arrayBuffer 重建 body+headers 转发），替换 `proxyToBackend` 与 `handleRequest` dev 分支的裸 `fetch(req)`，修 POST body 丢失；CACHE_NAME 沿用 v5（不涉及缓存语义）。`main.tsx` 注册改 v17。
- 验证：`node --check` v17 OK；改造6 内联脚本语法 0 错误；lint 0 错误。
- 用户后续步骤：①重新发布改造6（后端要拿到带加载器的新 HTML）②用 level3.js 重新提 levels 任务（不加 inject）③merge 后从 GamePlay「扩展」菜单进入（?ext= 透传）。dev-token 应变 200，提交者归属 test-003。
