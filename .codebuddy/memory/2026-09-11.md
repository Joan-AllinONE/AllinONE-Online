# 2026-09-11 工作日志（续）

## 发布托管模式自动升级（落实前日建议）【已完成，前端已部署 index-DSJFhD2s.js】

- 实现（`publishedGameService.savePublishedGame`，所有发布路径汇聚点）：
  `hostingType==='inline'` 且 `entryHtmlContent` >60KB 时自动升级为 `server`，并设置 `cdnUrl = ${GAMES_API_BASE}/${gameId}/files/${entryPoint||'index.html'}`（与 GamePlay buildGameFileUrl 同构，GAMES_API_BASE 已含 prod 绝对 URL 感知）。
- 逻辑链：saveGameFiles（stepUpload，云存储+manifest patch）先于 savePublishedGame（stepPublish）执行 → 升级时 manifest 已就绪；二次保存（handlePublishComplete）existing.hostingType 已是 server 不重复触发；GamePlay computeEffectiveGame 对 isModular 游戏会重算同款 cdnUrl ✓；审核预览 server 分支直连文件路由 ✓。
- ⚠️ 类型注意：`entryHtmlContent` 是运行时动态字段（不在 `PublishedGame` 类型上），需 `(newGame as any).entryHtmlContent` 访问。
- ⚠️ **环境教训（重要）**：execute_command 环境对长命令有 ~10s watch 监控，同步跑 vite build（50-60s）会被静默掐断且产物不更新；必须 `Start-Process node ... -PassThru`（分离进程）后**单独轮询 dist/assets hash 时间戳**确认完成。`cmd /c` 混合 PowerShell 语句解析也不可靠。
