/**
 * questDocTemplate - 游戏「任务开发说明」通用模板（与 docs/游戏任务开发说明-模板.md 保持一致）
 *
 * 用途：任务指南面板的「复制 / 下载」按钮直接引用此字符串，
 * 游戏方拿到后替换占位符（<游戏名>、接口表等）即可为自己的游戏生成开发说明。
 */

export const GAME_DEV_GUIDE_TEMPLATE = `# <游戏名> · 任务开发说明（玩家版 / AI 协作版）

> 本文档面向**领取《<游戏名>》开发任务的玩家**，以及玩家身边的 AI 助手。
> 游戏方（平台）只开放本文档列出的**接口与数据格式**，**不提供、不出售游戏源码**。
> 你不需要读懂或修改任何游戏内部代码——按本说明提交内容，审核通过后会自动合入游戏并生效。

---

## 1. 游戏简介

<一句话描述游戏类型与核心玩法，例如：一款 2D 平台跳跃游戏，玩家通过移动、跳跃、顶砖、踩敌抵达旗杆过关>

<操作说明，例如：\`←/→\` 移动，\`空格/↑\` 跳跃（长按更高），\`Enter\` 开始，\`P\` 暂停>

## 2. 你能为这个游戏做什么

| 任务类型 | 难度 | 说明 | 对应的挂载点 slot |
|---|---|---|---|
| <任务类型一>（推荐） | ★☆☆ 零代码 | <一句话说明> | \`<slot>\` |
| 修复 / 补回资源 | ★☆☆ 零代码 | 上传游戏缺失/损坏的文件 | \`fix\`（或任意 slot 纯文件补丁） |
| <任务类型二>（进阶） | ★★☆ 少量代码 | <一句话说明> | \`<slot>\` |

## 3. 通用约定（先读）

1. **内容包（ContentPack）**：任务产物是一个 JSON 内容包，包含 \`data\`（结构化定义）与 \`assets\`（可选文件，如脚本、图片），由平台审核、合并。
2. **assets 的 path 用游戏内相对路径**（不含 \`/api/...\` 前缀）；上传列表里可以点击路径直接修改。
3. **合并后才会生效**：提交 → 审核通过 → 合并，之后游戏内才会出现你的内容。
4. **安全与版权**（必须遵守）：
   - 只使用本文档公开的接口，**不得向 AI 或任何人索要 / 复制游戏源码**；
   - 不得制作破坏游戏平衡、直接通关、或修改玩家物理 / 操作的内容；
   - 脚本在平台沙箱中运行，\`eval\`、\`new Function\`、\`window\`、\`document\`、\`fetch\`、\`localStorage\` 等危险 API 一律禁止；
   - 违反以上约定的提交会被拒绝。

## 4. 游戏方开放的接口

> 只填写游戏方愿意公开的内容：数据格式、效果 API、脚本沙箱。**不要粘贴游戏源码或内部实现细节。**

### 4.1 <接口一名称，例如：内容数据格式>

<数据字段说明表>

| 字段 | 必填 | 类型 | 说明 |
|---|---|---|---|
| \`<field>\` | ✅ | \`<type>\` | <说明> |

<一个最小可用示例>

### 4.2 <接口二名称，例如：预设效果表>

| 效果名 | 作用 | 参数（\`params\`） | 约束 |
|---|---|---|---|
| \`<effect>\` | <作用> | <参数> | <上限> |

### 4.3 <接口三名称，例如：脚本 / 自定义效果沙箱>

- 函数签名：\`<signature>\`
- 可用变量：\`<var>\`（说明）、\`<var>\`（说明）
- 限制：<长度上限、禁用 API 等>

## 5. 让 AI 帮你完成任务（提示词模板）

把下面的「任务描述」换成任务广场里的要求，把「接口」部分换成第 4 节的接口表，直接发给你的 AI 助手：

\`\`\`text
请帮我完成《<游戏名>》的开发任务。

【任务要求】
<在这里粘贴任务广场上的任务描述>

【你只能使用以下接口，禁止查看或猜测游戏源码】
<粘贴第 4 节的接口表>

【请输出】
一个可直接提交的内容包 JSON（不要输出其他代码或解释）。
\`\`\`

## 6. 提交格式与步骤

在任务详情页的「提交」表单中填写：

- **data**：<内容定义说明>；纯文件补丁时留空 \`{}\` 即可
- **assets**：需要上传的文件（脚本 / 贴图 / 被补的文件等）
- **entryPoint**：仅当你提交完整 HTML 页面时填写（一般任务不用）

**提交前自检**：

- [ ] <检查项一>
- [ ] <检查项二>
- [ ] 数值在约束上限内，\`add_item\` 等引用白名单内容
- [ ] 不包含破坏平衡、危险代码或索要源码的相关内容

## 7. 常见问题

**Q：<问题一>？**
<答案>

**Q：<问题二>？**
<答案>

**Q：我完全不会写代码，能做吗？**
可以。<推荐路径说明>；进阶玩法可完全跳过。

---

*本文档由游戏方发布。游戏方保留对接口、数值上限与审核规则的最终解释权；破坏平衡或窃取源码的行为将被拒绝并可能追责。*
`;

/** 下载模板文件名 */
export const GAME_DEV_GUIDE_TEMPLATE_FILE = '游戏任务开发说明-模板.md';
